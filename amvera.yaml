meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10
build:
  requirementsPath: requirements.txt
  system_deps:
    - tesseract-ocr
    - tesseract-ocr-rus
run:
  command: python main.py
  persistenceMount: /data
  containerPort: 8080
